import React from 'react'
import styled from '@/styles/Sidebar.module.scss'
const Sidebar = () => {
  return (
    <section className={`${styled.main}`}>sidebar</section>
  )
}

export default Sidebar